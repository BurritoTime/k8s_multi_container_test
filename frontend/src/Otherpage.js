import React from 'react';
import {Link} from 'react-router-dom';

export default () => {
    return(
        <div>
            A generic test page--nothing here.
            <Link to="/"> Back Home </Link>
        </div>
    );
};